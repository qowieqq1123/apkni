# apkni

APK reverse-engineering workspace.

Contains decompiled assets (`out/`, `ZQZS/`), helper tools (`tools/`), and a backend server (`server/`).

> Note: large binary assets and decompiled output are committed as-is; individual files stay under GitHub's 100 MB limit.
